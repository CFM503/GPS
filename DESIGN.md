# 道路路产智能巡查与 GIS 管理系统 - 总体系统设计方案 (DESIGN.md)

---

## 1. 系统概述与设计理念

道路管理处辖区内道路沿线拥有大量的路产设施（交通标识牌、百米桩、里程碑、护栏、防撞设施、绿化林木、路灯、排水沟、桥涵、边坡、施工区及道路病害等）。传统巡查存在纸质记录繁琐、无法精确定位桩号、视频与照片脱节、断网数据丢失等痛点。

本系统旨在打造**“车载移动智能巡查 + Web GIS 数字化管理后台 + 云端协同服务”**的一体化闭环体系。核心理念为：**一次巡查，形成一份完整的道路数字档案**。

```
              ┌──────────────────────────────────────────────┐
              │           车载移动巡查端 (apps/mobile)        │
              │  - 驾驶友好一键秒级打点 (超大触控按键)        │
              │  - 持续高精度 GPS 轨迹 (1~3s 频次采集)       │
              │  - 5分钟自动切片录像 (VIDEO_001.mp4)         │
              │  - 本地离线数据库 (Offline-First SQLite)     │
              │  - 5级优先级断点续传队列 (断网/弱网自愈)      │
              └──────────────────────┬───────────────────────┘
                                     │ (HTTP REST / Resumable Upload)
                                     ▼
              ┌──────────────────────────────────────────────┐
              │            服务端核心层 (server)              │
              │  - 巡查 Session 聚合中枢 (PAT-20260907-0001) │
              │  - 轨迹点流式接收与 PostGIS LineString 拟合   │
              │  - 道路中心线空间拓扑与自动桩号换算 (K127+350)│
              │  - 视频绝对时间戳毫秒对齐计算器              │
              │  - 分块断点续传接收器 (Tus-like Upload-Offset)│
              │  - RBAC 权限与全链路操作审计日志              │
              └───────┬──────────────────────────────┬───────┘
                      │                              │
         ┌────────────▼─────────────┐   ┌────────────▼─────────────┐
         │ PostgreSQL 15 + PostGIS  │   │  MinIO / S3 对象存储池   │
         │ - 空间几何点线面存储     │   │ - 现场照片归档           │
         │ - 统一 WGS84 原始空间坐标│   │ - 视频切片分块对象存储   │
         └──────────────────────────┘   └──────────────────────────┘
                      │
                      ▼
              ┌──────────────────────────────────────────────┐
              │          Web GIS 管理后台 (apps/web)          │
              │  - MapProvider 抽象层 (解耦高德/百度/天地图)  │
              │  - 动态图层树 (道路/轨迹/各类路产/病害面)     │
              │  - 巡查回放控制器 (车辆平滑移动 + 时间轴)    │
              │  - 路产-视频毫秒对齐播放器 (点击直接跳帧)    │
              │  - 综合统计看板与多格式导出 (GeoJSON/KML/CSV)│
              └──────────────────────────────────────────────┘
```

---

## 2. 核心四大设计原则

### 原则 1：采集与网络彻底解耦 (Offline-First)
- 手机端/车载端在无网络、无 SIM 卡或弱网盲区时，**所有核心功能完全正常运转**（GPS 持续记录、录像分段写入、路产离线保存、本地照片落盘）。
- 所有采集记录带有本地生成的全局唯一 UUID 和本地状态 `PENDING`。
- 网络恢复后，由后台同步器自动按序提交，失败支持指数退避重试。

### 原则 2：地图底图与核心业务数据解耦
- 高德或百度仅作为前端视图图层（View Layer）的底图展示。
- **数据库永远保存 WGS84 原始经纬度坐标**。
- 无论未来切换至天地图、百度、高德或自建离线 GIS 切片服务器，业务数据零改动、零迁移。

### 原则 3：路产形态全面支持点 (Point)、线 (LineString)、面 (Polygon)
- **点 (POINT)**：交通标识牌、百米桩、里程碑、单株林木、路灯、消能防撞桶。
- **线 (LINESTRING)**：波形梁钢护栏、中央隔离护栏、路侧排水沟、路肩石、边沟。
- **面 (POLYGON)**：绿化隔离带、边坡防护区、养护施工区、大面积路面坑槽与龟裂病害区。

### 原则 4：路产类型动态可扩展
- 禁止硬编码资产类型，建立 `asset_types` 字典元数据，支持管理员在后台动态新增、修改图标、定义字段属性。

---

## 3. 核心实体模型与数据流转

### 3.1 巡查会话 (Patrol Session) 聚合根
一次巡查从点击“开始巡查”到“结束巡查”，生成一条全局唯一的 `patrol_session` 记录（编号规则：`PAT-YYYYMMDD-0001`）。

所有巡查过程中的子资源均与该 Session 强绑定：
* `patrol_tracks`：高精度 GPS 打点集合（含经纬度、瞬时速度、航向角、高度、定位精度、绝对时间戳）。
* `video_slices`：切片视频文件列表（记录文件名、文件体积、绝对开始时间、绝对结束时间、时长、MinIO存储路径）。
* `assets`：采集的路产记录（记录位置几何、类型、桩号、现场照片、对应所属视频及精确时间戳）。

### 3.2 视频-路产-GPS 毫秒级时间联动机制
1. 车载摄像头启动录像，生成切片 $V_1$：
   - 文件名：`VIDEO_PAT-20260907-0001_001.mp4`
   - 开始绝对时间：`2026-09-07 09:10:00.000`
   - 结束绝对时间：`2026-09-07 09:15:00.000`
2. 巡线员在 `09:12:35.450` 点击采集【限速60标识牌】：
   - 路产记录保存绝对时间戳 $T_{asset} = 1788772355450$。
3. 服务端或前端查询关联视频算法：
   - 匹配满足 $V.start\_time \le T_{asset} < V.end\_time$ 的切片 $V_1$。
   - 毫秒偏移量 $\Delta t = T_{asset} - V.start\_time = 155.45$ 秒（即 `00:02:35.450`）。
4. 在 Web GIS 点击该路产，视频播放器直接自动加载 $V_1$ 并 seek 至 `155.45s` 处播放，实现证据级场景复核。

### 3.3 桩号推算几何算法
公路桩号由千米数与米数组成（如 $K127+350 = 127350$ 米）。
1. 道路表 `roads` 存储道路中心线 `centerline_geom` (PostGIS 3D/2D LineString) 及起始桩号 `start_milepost` (米)。
2. 当路产点坐标 $P$ 上报时：
   $$\text{fraction} = \text{ST\_LineLocatePoint}(centerline\_geom, P)$$
   $$\text{projected\_point} = \text{ST\_ClosestPoint}(centerline\_geom, P)$$
   $$\text{distance\_from\_start} = \text{ST\_Length}(\text{ST\_LineSubstring}(centerline\_geom, 0, \text{fraction}))$$
   $$\text{calculated\_milepost} = start\_milepost + distance\_from\_start$$
3. 将计算值转换为标称桩号字符串：
   - 千米部分：$\lfloor calculated\_milepost / 1000 \rfloor$
   - 米部分：$\text{round}(calculated\_milepost \pmod{1000})$
   - 结果：`K127+350`。

---

## 4. 坐标体系与高精度转换设计

三层坐标隔离策略：
1. **持久化层**：统一 `WGS84`（EPSG:4326），GPS 硬件原始输出直接入库，绝不二次偏移污染。
2. **转换算法层 (`shared/coords`)**：内置标准无漂移克拉索夫斯基椭球投影转换（WGS84 $\leftrightarrow$ GCJ-02 $\leftrightarrow$ BD-09），运算误差 $< 0.1$ 米。
3. **视图渲染层 (`apps/web`)**：
   - 当底图采用高德地图时：调用 `wgs84ToGcj02(lng, lat)` 传入高德图层。
   - 当底图采用百度地图时：调用 `wgs84ToBd09(lng, lat)` 传入百度图层。
   - 当底图采用天地图或开源 OpenStreetMap 时：直接传入 WGS84 坐标。

---

## 5. 5级优先级离线同步与断点续传架构

移动巡查端设计 `SyncManager` 调度管道，严格按照数据重要度与体积分配上传优先级：

| 优先级 | 实体 | 单批数据量 | 上传策略 | 业务意义 |
| :---: | :---: | :---: | :---: | :--- |
| **P1** | **PatrolSession** | 约 1 KB | 实时优先，网络连通立即创建或更新会话摘要 | 后台管理人员第一时间获知巡查开启与任务状态 |
| **P2** | **GPSTrack** | 约 10~50 KB / 批 (50点) | 批量打包压缩上传 | Web GIS 实时/准实时描画车辆动态行驶轨迹 |
| **P3** | **Asset** | 约 2 KB / 条 | 逐条或小批量上报，自动触发服务端桩号计算 | 路产点位即刻在 GIS 地图上点亮 |
| **P4** | **Photo** | 约 500 KB ~ 2 MB | 并发线程池按序上传，关联路产主键 | 现场高清照片归档 |
| **P5** | **Video** | 约 50 MB ~ 200 MB / 切片 | **分块断点续传** (Chunk Size: 5MB)，支持限制 Wi-Fi 上传 | 视频大文件后台静默重试，绝不阻塞前端业务交互 |

### 视频断点续传协议规范：
- `POST /api/videos/upload/init`：初始化上传任务，返回 `upload_id` 与已上传偏移量 `offset`。
- `PATCH /api/videos/upload/chunk`：携带请求头 `Upload-Offset: 15728640`，传输分块数据，服务端校验 `offset` 后追加写入并持久化进度。
- `POST /api/videos/upload/complete`：分块全部写入后触发 MD5 完整性核验，正式转入 MinIO 并更新数据库切片记录。

---

## 6. 车载移动端驾驶安全人机工程学

巡查过程中保障巡线员人身与行车安全是本系统的核心生命线：
1. **超大触控块**：屏幕主区域平铺 6 个核心大按钮，触控靶心尺寸 $> 80 \times 80\text{ pt}$，边缘防误触：
   - `[ 标识牌 ]` (天蓝色)
   - `[ 百米桩 ]` (草绿色)
   - `[ 护栏 ]` (橙黄色)
   - `[ 林木 ]` (青绿色)
   - `[ 道路病害/异常 ]` (警示红)
   - `[ 其他设施 ]` (蓝灰色)
2. **零打字交互**：按键触发时震动/声音反馈，系统在 10 毫秒内捕获当前状态并离线入库。
3. **停车后补录机制**：巡查员靠边停车或巡查结束后，可在“待补录档案”列表中点击对应条目，补拍多角度高清照片、录音或补充文字备注文档。

---

## 7. Android 后台保活与异常防御

针对车载场景下的系统休眠、电池省电模式和后台切屏进行多级防御：
1. **前台服务 (Foreground Service)**：巡查开始后立即唤起带常驻通知栏的前台服务，展示当前巡查时长与采集计数，防止低内存杀进程。
2. **唤醒锁 (PARTIAL_WAKE_LOCK)**：巡查期间持有 CPU 唤醒锁，防止熄屏后 GPS 芯片休眠导致轨迹断线。
3. **定位漂移清洗**：GPS 服务端和客户端均加入卡尔曼滤波与水平精度过滤（`accuracy > 30` 米的点标记为可疑漂移点，不在巡查主轨迹中连线）。
